<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gary.spiders">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainMenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.EnlargeTextActivity" />
        <activity android:name=".activity.QuestionnaireActivity" />
        <activity android:name=".activity.PlayStopVideoActivity" />
        <!-- <activity -->
        <!-- android:name=".activity.MainMenuActivity" -->
        <!-- android:label="@string/title_activity_main_menu" -->
        <!-- android:theme="@style/AppTheme.NoActionBar"></activity> -->
        <activity android:name=".activity.ChooseGameActivity" />
        <activity android:name=".activity.MetricsActivity"></activity>
    </application>

</manifest>